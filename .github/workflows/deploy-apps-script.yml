name: Deploy Apps Script

on:
  push:
    branches: [main]
    paths:
      - 'projects/qa-dashboard/src/**'
      - 'projects/qa-test-management/src/**'
      - 'projects/mom-rolling-pic/src/**'
  workflow_dispatch:  # Allow manual trigger

jobs:
  deploy-dashboard:
    name: Deploy QA Dashboard
    runs-on: ubuntu-latest
    if: contains(github.event.head_commit.modified, 'projects/qa-dashboard/src')

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'

      - name: Install clasp
        run: npm install -g @google/clasp

      - name: Setup clasp credentials
        run: |
          echo "$CLASPRC_JSON" > ~/.clasprc.json
        env:
          CLASPRC_JSON: ${{ secrets.CLASPRC_JSON }}

      - name: Push to Apps Script (Dashboard)
        working-directory: ./projects/qa-dashboard
        run: clasp push --force

  deploy-test-management:
    name: Deploy QA Test Management
    runs-on: ubuntu-latest
    if: contains(github.event.head_commit.modified, 'projects/qa-test-management/src')

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'

      - name: Install clasp
        run: npm install -g @google/clasp

      - name: Setup clasp credentials
        run: |
          echo "$CLASPRC_JSON" > ~/.clasprc.json
        env:
          CLASPRC_JSON: ${{ secrets.CLASPRC_JSON }}

      - name: Push to Apps Script (Test Management)
        working-directory: ./projects/qa-test-management
        run: clasp push --force

  deploy-mom-rolling:
    name: Deploy MOM Rolling PIC
    runs-on: ubuntu-latest
    if: contains(github.event.head_commit.modified, 'projects/mom-rolling-pic/src')

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'

      - name: Install clasp
        run: npm install -g @google/clasp

      - name: Setup clasp credentials
        run: |
          echo "$CLASPRC_JSON" > ~/.clasprc.json
        env:
          CLASPRC_JSON: ${{ secrets.CLASPRC_JSON }}

      - name: Push to Apps Script (MOM Rolling)
        working-directory: ./projects/mom-rolling-pic
        run: clasp push --force
